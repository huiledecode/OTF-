extends layout

append head
    script(src='/javascripts/user_socket.js')

block content
    p Liste des Utilisateurs :
        select
            each user, i in result
                option(value="#{user._id}") #{user.login}-#{user.password}
