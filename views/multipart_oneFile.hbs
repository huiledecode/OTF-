{{#content "head"}}
    {{> head}}
{{/content}}
{{#content "nav"}}
    {{> nav}}
{{/content}}
{{#content "page"}}
    <script type="text/javascript">
        function readURL(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();
            var nom_id = (input.value.match(/[^\/\\]+$/gi)[0]).substring(0,(input.value.match(/[^\/\\]+$/gi)[0]).length-4);
            reader.onload = function (e) {
                $('#img_prev').attr('src', e.target.result);
                // $('#'+(input.value.match(/[^\/\\]+$/gi)[0]).substring(0,(input.value.match(/[^\/\\]+$/gi)[0]).length-4))
                //.attr('src', e.target.result);
                // alert('e.target.result : ' + e.target.result);

            };
            reader.readAsDataURL(input.files[0]);
        }
        else {
            var img = input.value;
            $('#img_prev').attr('src',img).height(160);
        }

        $("#x").show().css("margin-right","10px");
    }
</script>
	<!-- Page Content -->
    <div class="container-fluid">
    <div class="row-fluid">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3>Uploader un fichier image sur le serveur dans le dossier './public/uploads'</h3>
            </div>
            <div class="panel-body">
                <div class="row">
                    <form role="form" action="/uploadFile" method="POST" enctype="multipart/form-data">
                        <div class="col-lg-6">
                            <div class="form-group">
                                <label>Veuillez choisir une photo de portrait qui sera automatiquement redimensionnée.</label>
                                <input type="file" id="T7" name="thumbnail" onchange="readURL(this);"> <br>
                                <img id="img_prev">
                            </div>
                        </div>
                        <!-- /.col-lg-6 (nested) -->
                        <div class="col-lg-6">
                            <button type="submit" class="btn btn-default" value="Envoyer">Envoyer</button>
                            <button type="button" onclick="javascript:history.go(-1);" class="btn btn-default" value="Annuler">Annuler</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
	</div>
    </div><!-- /Page Content -->
{{#content "footer"}}
    {{> footer}}
{{/content}}
{{/content}}
